export type UserModelType = {
    id?: number
    fullName: string
    username: string
    email: string
    dni: string
    sex: string
    address: string
    dob: Date
    dniExpiration: Date
    createdAt: Date
    updatedAt: Date
}